node_modules/
dist/
coverage/
*.tsbuildinfo
*.log

# API Pilot workspace caches (responses, history, spec cache)
.apipilot/.cache/
.apipilot/environments.local.yaml

# Scratch specs downloaded for a local check against something real.
tests/fixtures/specs/local-*.yaml

# Machine-local agent permissions, not shared config.
.claude/settings.local.json

.DS_Store
Thumbs.db
